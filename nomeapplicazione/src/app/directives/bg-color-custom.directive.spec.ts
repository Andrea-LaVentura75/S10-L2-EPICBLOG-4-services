import { BgColorCustomDirective } from './bg-color-custom.directive';

describe('BgColorCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new BgColorCustomDirective();
    expect(directive).toBeTruthy();
  });
});
